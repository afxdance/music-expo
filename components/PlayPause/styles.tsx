import { StyleSheet } from 'react-native';

const styles = StyleSheet.create(
  {
    container: {
      backgroundColor: '#fff',
      borderLeftWidth: 10,
      borderLeftColor: '#ffffff',
      size: 30,
    },
  }
);

export {styles};