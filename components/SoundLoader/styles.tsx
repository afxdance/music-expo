import { StyleSheet } from 'react-native';

const styles = StyleSheet.create(
  {
    container: {
      backgroundColor: '#fff',
      borderLeftWidth: 0,
      borderLeftColor: '#ffffff',
      size: 60,
      alignSelf: 'flex-end',
      position: 'absolute',
        top: 50,

    },
  }
);

export {styles};